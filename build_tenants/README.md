# Build Tenants

The canonical build-tenant system surface is [TENANT_REGISTRY.md](/Users/jim/src/apps/genesis_sdlc/build_tenants/TENANT_REGISTRY.md).

Use that file for:

- realization topology
- `1 specification -> n build tenants`
- tenant status and registry state
- shared vs tenant-local boundary rules
