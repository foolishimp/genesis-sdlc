# Tenant Design

Tenant-local design surfaces live here.

Use `.gsdlc/release/operating-standards/` as the installed method reference, `build_tenants/common/design/` for shared realization law, and `.gsdlc/release/design/` for the installed framework design reference.

## F_P Customization

Use `design/fp/` to describe tenant-local F_P tuning for bounded constructive turns.
