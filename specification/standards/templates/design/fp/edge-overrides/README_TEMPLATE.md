# Edge Overrides

Put per-edge F_P override files here.

## Naming

Use the installed helper to discover the effective edge name and the expected override filename shape.

## Rule

Only create overrides for edges that need project-specific tuning. Absent overrides fall back to the installed defaults.
