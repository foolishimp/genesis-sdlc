# Genesis kernel default — written by gen-install.py v1.0.1
#
# This file is the engine's bootstrap config. Domain installers
# set package, worker, and runtime_contract when they install.
# Without a domain package, the engine has no graph to iterate.
#
runtime_contract: .gsdlc/release/genesis.yml
# package: your_domain.package:package
# worker:  your_domain.package:worker
