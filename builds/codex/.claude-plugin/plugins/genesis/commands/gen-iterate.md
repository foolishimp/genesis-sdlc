# /gen-iterate — Run One Iteration

Run a single engine iteration over the selected scope.

```bash
PYTHONPATH=.genesis python -m genesis iterate --workspace . [--feature REQ-F-*] [--edge "source→target"]
```
